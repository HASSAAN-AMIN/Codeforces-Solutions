/* 
                بِسْمِ اللهِ الرَّحْمٰنِ الرَّحِيْمِ   
    In the name of Allah, the Most Gracious, the Most Merciful.
*/
#include <iostream>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int m ,s;
    cin >> m >> s;
    // (1 ≤ m ≤ 100 )  ( 0 ≤ s ≤ 900)
    // length m , sum of digits s
    // output minimum and maximum such num
    int x , y = 0 ;
    // if the number is  a1a2a3...am 
    // sum of digits is a1 + a2 + a3 +a4 = s

    // brute force is simple and working
    // but lets try dp and so optimizing ofc

    
    return 0;
}